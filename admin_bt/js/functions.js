function validateNewExamForm(numOfquestions) {
    var fields = [];
    txtAreaName = 'Number';

    for (var i = 0; i <= numOfquestions; i++) {
        fields.push(txtAreaName.i);
    }

var i, j = fields.length, fieldname;

    for (i = 0; i <= numOfquestions; i++) {
        fieldname = fields[i];
        if(document.forms["register"][fieldname].value === ""){
            
        }
    }

}